[project]
name = "aiops"
version = "0.1.0"
description = "Scalable multi-agent system powered by LangChain/LangGraph and Gemini"
requires-python = ">=3.11"
dependencies = [
    # LangChain core
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langchain-community>=0.3",
    "langchain-google-genai>=2.0",
    "langchain-openai>=0.2",
    # LangGraph for agent orchestration
    "langgraph>=0.2",
    # MCP (Model Context Protocol) adapter for LangChain tools
    "langchain-mcp-adapters>=0.1",
    # Monitoring
    "langfuse>=3.0",
    # Logging
    "loguru>=0.7",
    # Configuration
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    # Claude Code Agent SDK (task delegation)
    "claude-agent-sdk>=0.1",
    # Utilities
    "httpx>=0.27",
    # SCIP agent
    "neo4j>=5.0",
    # RepoMap agent
    "tree-sitter>=0.23",
    "tree-sitter-python>=0.23",
    "tree-sitter-java>=0.23",
    "tree-sitter-kotlin>=0.23",
    "tree-sitter-typescript>=0.23",
    "tree-sitter-javascript>=0.23",
    "networkx>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "mypy>=1.8",
    "ruff>=0.3",
]

[project.scripts]
aiops = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
